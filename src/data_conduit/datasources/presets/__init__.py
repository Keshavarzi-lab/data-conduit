"""
Preset subpackage for data-conduit datasources.

Presets are optional, dependency-specific wrappers built on top of the core
datasource classes. Each sub-package corresponds to a specific acquisition
system and its required dependencies.

Sub-packages
------------
harp
    HARP-style binary data presets. Requires ``harp-python``.
"""
