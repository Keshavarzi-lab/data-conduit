[project]
name = "data-conduit"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "CS-2425", email = "callum.marshall.21@ucl.ac.uk" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.10.4,<0.11.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mkdocs>=1.6.1",
    "mkdocs-material>=9.7.2",
    "pre-commit>=4.5.1",
    "pytest>=9.0.2",
    "ruff>=0.15.2",
]
